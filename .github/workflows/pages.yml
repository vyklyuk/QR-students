name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'web/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      # URL веб-застосунку Apps Script ніколи не потрапляє в git — його підставляє
      # секрет репозиторію APPS_SCRIPT_URL просто в момент збірки (CLAUDE.md, правило 5).
      - name: Generate web/config.local.js from secret
        run: echo "const APPS_SCRIPT_URL = '${{ secrets.APPS_SCRIPT_URL }}';" > web/config.local.js

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
